{
  "name": "pine-script-extension-server",
  "description": "Pine Script v6 Language Server",
  "version": "1.1.0",
  "author": "Revanth Pobala",
  "main": "./out/server.js",
  "scripts": {
    "copy-tests": "mkdir -p out/tests && cp src/tests/cases.json out/tests/cases.json",
    "compile": "tsc -b && npm run copy-tests",
    "watch": "tsc -b -w"
  },
  "dependencies": {
    "web-tree-sitter": "^0.26.3",
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}